# 微信小程序演员详情页生成 Agent 规格文档

本文档用于交给 Codex 或其他代码执行型 Agent，指导其实现一个“演员详情页视觉生成与小程序落地 Agent”。该 Agent 的核心任务是：根据演员资料、参考人物图、参考模板图，生成适合微信小程序详情页展示的国风演员详情页视觉方案，并优先以“小程序原生组件渲染文字 + 图像模型生成视觉资产”的方式落地。

## 1. 目标

实现一个 Agent，使其能够自动完成以下工作：

1. 读取用户提供的演员资料、人物参考图、模板参考图。
2. 判断输出目标是“微信小程序详情页”，而不是传统海报。
3. 生成适合 `gpt-image2` 或项目内图像生成接口使用的视觉提示词。
4. 避免让图像模型直接生成大量中文正文。
5. 将真实文字、电话、微信号、二维码、按钮等交给小程序原生组件渲染。
6. 如果项目是小程序代码仓库，则按照现有技术栈实现演员详情页页面或组件。
7. 输出可验收的页面、提示词、数据结构和说明。

## 2. 核心原则

### 2.1 图像模型只负责视觉，不负责长文本

必须优先采用以下方案：

```text
图像模型生成：人物主视觉、古风背景、装饰纹理、照片墙风格、作品缩略图风格
小程序渲染：姓名、身高、体重、技能、作品、简介、联系方式、按钮、二维码
```

不要把整张详情页的所有文字都交给图像模型生成。原因：

1. 中文容易乱码。
2. 数字、姓名、电话、微信号容易错误。
3. 二维码不可保证可扫。
4. 不利于小程序动态更新演员资料。
5. 小程序用户无法复制文字、点击按钮或进行交互。

### 2.2 页面定位

该页面是：

```text
微信小程序演员详情页
移动端竖向滚动信息页
演员资料展示页
国风影视演员个人主页
```

该页面不是：

```text
单张传统宣传海报
杂志封面
PC 网站落地页
纯图片长图
不能交互的静态图
```

### 2.3 版式优先于风格

提示词必须明确“固定模块结构”。不要只写“古风、国风、演员海报”。必须锁定以下模块：

1. 顶部人物主视觉。
2. 基础资料卡。
3. 技能特长。
4. 代表作品。
5. 更多形象。
6. 个人简介。
7. 数据统计。
8. 底部联系或底部操作区。

## 3. 输入

Agent 应支持以下输入。

### 3.1 必填输入

```json
{
  "actorName": "许金铭",
  "height": "168cm",
  "weight": "45kg",
  "location": "横店",
  "skills": ["威亚", "游泳", "国画", "舞蹈", "模特"],
  "sellingPoints": [
    "配合度高，不玻璃心，新剧持续上线中！🔥",
    "抖音粉丝 33.8w（可以帮剧宣）",
    "妆造驾驭类型多，可塑性强"
  ],
  "referenceImages": {
    "template": "参考模板图路径或 URL",
    "person": ["人物参考图路径或 URL"]
  }
}
```

### 3.2 推荐输入

```json
{
  "residentLocation": "横店",
  "works": [
    {
      "title": "长安风华",
      "role": "赵清妍",
      "year": "2024年",
      "tags": ["古装", "爱情", "权谋"],
      "cover": "作品缩略图路径或 URL"
    }
  ],
  "bio": [
    "热爱表演，镜头前真诚而有力量，能够快速进入状态，适应不同类型的角色挑战。",
    "不断学习与突破，提升演技与综合素质，期待更多合作机会，共同创作优秀作品。"
  ],
  "stats": [
    { "label": "参演作品", "value": "12+" },
    { "label": "抖音粉丝", "value": "33.8W+" },
    { "label": "社媒粉丝", "value": "50W+" },
    { "label": "合作满意度", "value": "98%" }
  ],
  "contact": {
    "phone": "138-8888-8888",
    "wechat": "xjm_0518",
    "qrCode": "二维码图片路径或 URL"
  }
}
```

### 3.3 可选输入

```json
{
  "style": "古风国风",
  "primaryColor": "#263B2C",
  "accentColor": "#BDA66A",
  "pageWidth": "750rpx",
  "outputMode": "miniapp-components",
  "imageModel": "gpt-image2"
}
```

## 4. 输出

Agent 最终应输出以下内容。

### 4.1 如果在代码仓库中执行

根据项目技术栈输出对应文件：

1. 原生微信小程序：`.wxml`、`.wxss`、`.js` 或 `.ts`、`.json`。
2. Taro：React/Vue 页面组件、样式文件、数据 mock。
3. uni-app：`.vue` 页面或组件。
4. 其他框架：遵循项目已有目录、组件、状态管理和样式规范。

### 4.2 如果只生成方案

至少输出：

1. `actor-detail-agent-prompt.md`：图像生成提示词。
2. `actor-detail-data.schema.json`：演员资料数据结构。
3. `actor-detail-page-spec.md`：小程序详情页结构说明。
4. 如有需要，输出视觉资产清单。

## 5. Agent 执行流程

### 5.1 第一步：识别项目环境

Codex 必须先检查项目结构：

1. 查找 `app.json`、`project.config.json`、`package.json`。
2. 判断是原生小程序、Taro、uni-app、mpvue、Remax、还是其他框架。
3. 查找现有页面目录、组件目录、样式变量、图标库、图片资源目录。
4. 遵循现有项目写法，不另起一套架构。

如果当前目录不是项目仓库，则只生成文档和提示词，不创建小程序代码。

### 5.2 第二步：解析演员资料

将用户输入标准化为 `ActorProfile`。

TypeScript 结构建议：

```ts
export interface ActorWork {
  title: string;
  role?: string;
  year?: string;
  tags?: string[];
  cover?: string;
}

export interface ActorSkill {
  name: string;
  description?: string;
  icon?: string;
}

export interface ActorStat {
  label: string;
  value: string;
  icon?: string;
}

export interface ActorContact {
  phone?: string;
  wechat?: string;
  qrCode?: string;
}

export interface ActorProfile {
  actorName: string;
  height?: string;
  weight?: string;
  location?: string;
  residentLocation?: string;
  skills: ActorSkill[];
  sellingPoints: string[];
  works: ActorWork[];
  photos: string[];
  bio: string[];
  stats: ActorStat[];
  contact?: ActorContact;
  referenceImages?: {
    template?: string;
    person?: string[];
  };
}
```

### 5.3 第三步：选择输出模式

默认模式必须是：

```text
miniapp-components
```

含义：

```text
用小程序组件渲染真实文字和交互；
用图像模型生成视觉背景和照片资产。
```

只有在用户明确要求“生成一整张长图”时，才使用：

```text
single-long-image
```

### 5.4 第四步：生成图像提示词

Agent 需要生成两类提示词。

#### A. 视觉模板提示词

用于生成不含真实长文本的页面视觉背景。

```text
生成一张用于微信小程序演员详情页展示的竖版视觉模板，宽度为 750px，移动端手机屏幕浏览，适合在小程序详情页中作为背景和视觉资产使用。

整体不是传统宣传海报，而是微信小程序演员详情页 UI 视觉稿。请保持模块化、信息流式、可滚动阅读的排版结构，留出清晰文字区域，方便后续用小程序真实文字叠加。

整体风格：
古风国风、影视演员资料详情页、米白宣纸质感背景、淡墨山水、江南亭台楼阁、竹叶装饰、浅金色点缀、深墨绿色底部区域、古典细线边框、清雅高级、商业化、移动端友好、干净留白。

画布与布局：
750px 宽，竖版长图，高度约 1800px 到 2200px，适合微信小程序详情页滚动展示。左右安全边距 40px。所有信息模块垂直排列，间距统一，不拥挤。

页面结构固定如下：
1. 顶部主视觉区域，高度约占页面 32%。右侧放一位年轻中国女演员古装半身写真，黑色长发，精致古典发饰，浅色汉服或唐风古装，手持浅金色团扇，气质清冷温婉，五官清晰自然。背景为淡墨山水、亭台楼阁、竹叶和柔和光影。左侧预留姓名标题区域和核心卖点区域，但不要生成具体文字，只保留优雅留白和浅色文本占位线。
2. 顶部主视觉下方放一个米白色圆角资料卡，古典细线边框，轻微阴影。资料卡内分为两列信息布局，预留身高、体重、坐标、常驻地、技能等文字位置。每项左侧使用简洁线性图标。不要生成真实文字。
3. 技能特长模块为列表结构，共 5 个列表项，每项左侧为圆形浅灰图标，右侧为两行文字占位。不要生成真实文字。
4. 代表作品模块共 3 个作品条目。每个条目左侧是古装剧照风格缩略图，右侧预留剧名、角色名、年份类型的文字区域。
5. 更多形象模块横向排列 4 到 6 张竖版圆角照片卡片，展示同一位女演员不同古装造型，包括白色古装、粉色古装、红色华服、蓝色持伞造型、夜景古装。
6. 个人简介模块左侧大面积留白用于小程序渲染真实简介文字，右侧或底部使用淡墨山水、桥、亭台、飞鸟装饰。
7. 数据统计模块为一条浅金色统计栏，分为 4 个等宽模块，每个模块上方有线性图标，下方预留数字和标签文字位置。不要生成真实文字。
8. 页面底部为深墨绿色区域，带古建筑屋檐和金色云纹装饰。预留联系方式、微信号、二维码或按钮区域。

人物要求：
参考人物为年轻中国女性，古典气质，黑色长发，五官精致，妆容自然，不要欧美脸，不要动漫脸，不要夸张网红脸。不同照片中保持同一人物特征。

UI 要求：
这是微信小程序详情页视觉模板，不是杂志封面，不是营销落地页，不是横版海报。整体要适合手机屏幕阅读，文字区域必须干净，模块边界清楚，卡片圆角不超过 8px，留白充足，不要出现复杂纹理覆盖文字区域。

禁止：
不要生成大段真实中文文字，不要生成乱码，不要生成无意义英文，不要生成不可读的小字，不要让人物遮挡信息区，不要改变为现代商务风，不要使用赛博朋克风，不要使用卡通动漫风，不要使用纯深色背景，不要让页面过度拥挤。
```

#### B. 顶部人物主视觉提示词

用于单独生成顶部 hero 图，适合小程序作为背景图或封面图。

```text
生成一张微信小程序演员详情页顶部主视觉图，尺寸 750px x 680px，古风国风影视演员资料页风格。

画面右侧是一位年轻中国女演员古装半身写真，黑色长发，精致古典发饰，浅色汉服或唐风古装，手持浅金色团扇，气质清冷温婉，五官自然清晰。人物不要过度磨皮，不要欧美脸，不要动漫脸。

画面背景为米白宣纸质感、淡墨山水、江南亭台楼阁、竹叶装饰、柔和光影、浅金色点缀。左侧和下方保留干净留白，用于小程序叠加演员姓名、卖点和资料卡。

不要生成文字，不要生成 logo，不要生成二维码，不要让人物遮挡左侧文字区。移动端友好，清晰，高级，商业演员宣发质感。
```

#### C. 单张长图提示词，仅作为备选

只有当用户明确要求“直接生成完整长图”时才使用。

```text
生成一张用于微信小程序演员详情页展示的竖版长图，宽度 750px，高度约 2000px，适合手机端滚动浏览。整体为古风国风演员资料详情页，不是传统海报。

请严格使用移动端详情页排版：顶部人物主视觉，下方基础资料卡，技能特长，代表作品，更多形象，个人简介，数据统计，底部联系区域。所有文字必须尽量清晰可读，字号适合手机屏幕，不要生成过小文字。

内容由输入数据填充。姓名、数字、联系方式、微信号、二维码必须尽量准确。但如果准确性非常重要，应改由小程序原生组件叠加。

视觉风格：
古风国风、米白宣纸背景、淡墨山水、江南亭台、竹叶装饰、浅金色高亮、深墨绿色底部区域、古典细线边框、商业演员资料页、清雅高级、移动端详情页 UI、留白充足、中文排版清晰。

严格要求：
适配微信小程序详情页，750px 宽，竖向滚动，信息模块清晰。不要生成横版，不要生成单张封面图，不要遗漏模块，不要让文字压在人脸上，不要让背景影响文字阅读，不要生成乱码，不要生成多余人物，不要使用现代霓虹风、赛博朋克风、动漫风。
```

### 5.5 第五步：实现小程序页面

如果当前仓库是小程序项目，Codex 应实现或更新一个演员详情页。

建议页面结构：

```text
pages/actor-detail/
  index.wxml
  index.wxss
  index.ts 或 index.js
  index.json
```

如果项目已有页面命名规范，则遵循项目规范。

页面模块建议：

1. `hero-section`
2. `profile-card`
3. `skill-section`
4. `works-section`
5. `photos-section`
6. `bio-section`
7. `stats-section`
8. `bottom-action-bar`

### 5.6 第六步：样式规范

小程序样式建议：

```text
页面设计宽度：750rpx
左右安全边距：32rpx 到 40rpx
顶部主视觉高度：560rpx 到 680rpx
卡片圆角：8rpx 到 16rpx
正文字号：26rpx 到 30rpx
模块标题字号：34rpx 到 44rpx
姓名标题字号：56rpx 到 72rpx
模块间距：24rpx 到 36rpx
底部固定操作栏高度：96rpx 到 120rpx
底部安全区：env(safe-area-inset-bottom)
```

配色建议：

```text
背景色：#F5F0E6
主文字：#262318
次级文字：#6F6758
主色：#263B2C
强调色：#BDA66A
边框：rgba(120, 100, 70, 0.24)
卡片：rgba(255, 252, 244, 0.88)
```

### 5.7 第七步：交互规范

必须支持：

1. 点击电话，调用小程序拨号能力。
2. 点击微信号，复制到剪贴板。
3. 点击二维码，可预览二维码图片。
4. 点击更多形象图片，可预览大图。
5. 点击分享，可调用小程序分享。
6. 底部操作栏适配安全区。

### 5.8 第八步：空数据处理

Agent 必须考虑以下情况：

1. 没有作品时，隐藏代表作品模块或展示空状态。
2. 没有二维码时，不显示二维码区域。
3. 没有电话时，不显示拨号按钮。
4. 没有简介时，不显示个人简介模块。
5. 图片加载失败时显示占位图。
6. 技能没有说明时，只显示技能名称。

## 6. 推荐页面数据示例

```json
{
  "actorName": "许金铭",
  "height": "168cm",
  "weight": "45kg",
  "location": "横店",
  "residentLocation": "横店",
  "skills": [
    { "name": "威亚", "description": "高空吊威亚经验丰富" },
    { "name": "游泳", "description": "游泳健身，水性良好" },
    { "name": "国画", "description": "擅长写意国画" },
    { "name": "舞蹈", "description": "古典舞、民族舞、现代舞" },
    { "name": "模特", "description": "平面拍摄，走秀经验" }
  ],
  "sellingPoints": [
    "配合度高，不玻璃心，新剧持续上线中！🔥",
    "抖音粉丝 33.8w（可以帮剧宣）",
    "妆造驾驭类型多，可塑性强"
  ],
  "works": [
    {
      "title": "长安风华",
      "role": "赵清妍",
      "year": "2024年",
      "tags": ["古装", "爱情", "权谋"]
    },
    {
      "title": "锦绣未央",
      "role": "沈云菱",
      "year": "2023年",
      "tags": ["古装", "励志", "情感"]
    },
    {
      "title": "花间令",
      "role": "苏莞儿",
      "year": "2022年",
      "tags": ["古装", "悬疑", "情感"]
    }
  ],
  "bio": [
    "热爱表演，镜头前真诚而有力量，能够快速进入状态，适应不同类型的角色挑战。",
    "不断学习与突破，提升演技与综合素质，期待更多合作机会，共同创作优秀作品。"
  ],
  "stats": [
    { "label": "参演作品", "value": "12+" },
    { "label": "抖音粉丝", "value": "33.8W+" },
    { "label": "社媒粉丝", "value": "50W+" },
    { "label": "合作满意度", "value": "98%" }
  ],
  "contact": {
    "phone": "138-8888-8888",
    "wechat": "xjm_0518",
    "qrCode": ""
  }
}
```

## 7. 给 Codex 的实现指令

Codex 执行时必须遵守：

1. 先读项目结构，再决定实现方式。
2. 如果已有页面组件体系，复用现有组件和样式规范。
3. 不要把长中文文字写进图片生成提示词里作为最终可读文字。
4. 真实信息必须来自数据对象，由小程序原生文字渲染。
5. 图片资产路径必须可配置，不要硬编码临时本地路径。
6. 页面宽度按 `750rpx` 设计，适配不同手机屏幕。
7. 底部操作栏必须处理安全区。
8. 不要引入大型依赖，除非项目已有对应依赖。
9. 不要重构无关代码。
10. 实现后必须进行可运行性检查。

## 8. 验收标准

### 8.1 视觉验收

1. 页面第一屏能明显看到演员主视觉和姓名。
2. 页面不是传统海报，而是小程序详情页。
3. 模块边界清晰，滚动阅读自然。
4. 背景不影响正文阅读。
5. 文字没有压住人物脸部。
6. 页面整体为古风国风，但不过度装饰。

### 8.2 数据验收

1. 演员姓名正确。
2. 身高、体重、坐标、常驻地正确。
3. 技能列表正确。
4. 代表作品正确。
5. 简介正确。
6. 电话、微信号、二维码不由图像模型伪造。

### 8.3 交互验收

1. 电话按钮能拨号。
2. 微信号能复制。
3. 图片能预览。
4. 分享入口可用。
5. 底部按钮不被 iPhone 底部安全区遮挡。

### 8.4 工程验收

1. 页面能编译。
2. 没有明显 TypeScript 或 ESLint 错误。
3. 没有破坏现有路由。
4. 没有使用不存在的图片资源。
5. 样式没有污染全局页面。

## 9. Agent 对用户的提问规则

只有缺少关键信息时才提问。最多一次问 3 个问题。

优先询问：

1. 是要“生成小程序页面代码”，还是只要“图像生成提示词”？
2. 项目技术栈是原生小程序、Taro、uni-app，还是其他？
3. 是否需要保留联系方式、二维码和底部联系按钮？

不要因为以下信息缺失而阻塞：

1. 作品名缺失，可使用占位作品或隐藏模块。
2. 简介缺失，可生成通用简介或隐藏模块。
3. 统计数据缺失，可隐藏统计模块。
4. 部分图片缺失，可使用占位图。

## 10. 最终交付格式

Codex 完成后，应向用户说明：

1. 修改或新增了哪些文件。
2. 页面入口在哪里。
3. 图像生成提示词在哪里。
4. 哪些内容需要用户替换真实素材。
5. 已执行哪些验证。
6. 如果未能运行验证，说明原因。

示例：

```text
已完成微信小程序演员详情页模板：
- 新增 pages/actor-detail/index.wxml
- 新增 pages/actor-detail/index.wxss
- 新增 pages/actor-detail/index.ts
- 新增 docs/actor-detail-image-prompt.md

页面采用小程序原生文字渲染演员资料，图像模型只用于顶部主视觉和背景资产，避免中文乱码和联系方式错误。

需要替换的素材：
- 顶部主视觉图
- 更多形象照片
- 作品缩略图
- 二维码图片

已检查：
- 页面数据结构
- 空数据展示
- 底部安全区样式
```

## 11. 一句话版本

如果只需要给图像生成 Agent 一个短目标，使用下面这句：

```text
为微信小程序生成演员详情页视觉资产和页面模板：750rpx 宽移动端竖向滚动页面，古风国风风格，顶部古装人物主视觉，下方模块化资料卡、技能、作品、形象照片、简介、数据统计和底部联系区；图像模型只生成背景、人物和装饰，不生成长中文正文；姓名、资料、电话、微信、二维码和按钮由小程序原生组件渲染，保证内容准确、可复制、可点击、可动态复用。
```
