# 00-155 需求

## 目标

移除 `pkg-card/actor-card/index` 小程序卡片预览底部的 `复制链接` 按钮。

## 问题

- 当前卡片预览底部同时展示 `复制链接` 和 `发送给好友`。
- 小程序卡片分享应通过微信 `open-type="share"` 调起分享面板。
- `复制链接` 暴露的是内部路径，不符合当前 MVP 的用户操作预期。

## 验收标准

- `artifact=miniProgramCard` 时底部只展示 `发送给好友`。
- `artifact=miniProgramCard` 不展示 `复制链接`。
- `artifact=poster` 保留 `保存相册` 和 `分享到朋友圈`。
- 不恢复旧配置流程，不新增兜底旧入口。
- 类型检查、小程序构建、包体审查通过。
- 源码和构建产物审查通过，评分不低于 95。
