# 00-151 任务清单

## 1. 页面流程

- [x] 新增 `actor-card` 编辑态三步状态。
- [x] 第 1 步只显示卡片预览。
- [x] 第 2 步只显示快速调节。
- [x] 第 3 步显示内容配置和保存动作。
- [x] 第 1/2 步底部主按钮不触发保存。

## 2. 底部栏层级

- [x] 提升 `KpBottomActionBar` 通用层级。
- [x] 提升 `actor-card` 页面底部栏层级。
- [x] 底部栏背景改为不透明，避免内容穿透。
- [x] 增加内容底部留白，避免最后内容被固定栏压住。

## 3. 审查

- [x] `npm run type-check`。
- [x] `npm run build:mp-weixin`。
- [x] `npm run audit:mp-package`。
- [x] 源码正向审查。
- [x] 构建产物正向审查。
- [x] 审查结果写入执行记录。
