# 00-153 任务

- [x] 梳理并确认 `card-list` 到 `actor-card` 的当前跳转。
- [x] 将 `card-list` 预览入口改为 `mode=preview`。
- [x] 将 `actor-card` 默认收敛为预览模式，移除用户可见二次三步配置。
- [x] 移除 `actor-card` 卡片模式下的海报机构名称强制校验。
- [x] 清理 MVP 主流程中的 `保存配置` / `保存分享配置` 文案。
- [x] 执行类型检查、小程序构建、包体审查。
- [x] 审查源码和构建产物，确认评分不低于 95。
