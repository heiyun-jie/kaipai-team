- [x] T1 新增独立 `00-80` Spec，并把当前问题明确收口为 `users/index` 表格区密度，不再继续混入 `00-79`
  - **Validates: Requirements 3.1 R1-R3, 3.4 R9-R11**
- [x] T2 在 `UserCenterView.vue` 中完成主表行高、单元格层级与固定操作列局部收口，不改变字段语义
  - **Validates: Requirements 3.2 R4-R6, 3.3 R7-R8**
- [x] T3 完成 `type-check / build / runtime(browser)` 三层验证，并把 `users/index` 表格区修复前后证据回填 `execution.md`
  - **Validates: Requirements 3.1 R2-R3, 3.4 R10-R11**
