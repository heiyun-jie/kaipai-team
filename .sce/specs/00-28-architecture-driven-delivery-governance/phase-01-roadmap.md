# 00-28 Phase 01 路线图

## 目标

把 `00-28` 从“推进原则”推进到“可直接排开发”的第一轮执行包。

本轮不直接修改业务代码，先建立 4 张能力切片卡，作为后续多线程推进的统一入口：

1. 实名认证闭环
2. 邀请裂变与邀请资格闭环
3. 会员能力与模板配置闭环
4. AI 简历润色闭环

## 本轮交付物

- `slices/verify-capability-slice.md`
- `slices/invite-referral-capability-slice.md`
- `slices/membership-template-capability-slice.md`
- `slices/ai-resume-polish-capability-slice.md`

## 当前结果

本轮 4 张能力切片卡已产出，可直接作为下一轮项目推进入口：

1. 实名认证闭环
2. 邀请裂变与邀请资格闭环
3. 会员能力与模板配置闭环
4. AI 简历润色闭环

按架构反推后的第一轮开发顺序建议为：

1. 实名认证闭环
2. 邀请裂变与邀请资格闭环
3. 会员能力与模板配置闭环
4. AI 简历润色闭环

下一步建议：

- 先从“实名认证闭环”继续拆成前端 / 后端 / 后台 / 联调四张执行卡
- 再按同样方法处理邀请和会员模板
- AI 简历润色保留在第三顺位之后推进真实实现

## 本轮推进原则

1. 每张切片卡都必须覆盖数据、后端、后台、小程序、联调、验收。
2. 每张切片卡都必须标明上游依赖和当前阻塞项。
3. 每张切片卡都必须给出建议推进顺序，而不是只列模块清单。
4. 若某条能力本轮只能做到局部完成，必须明确“局部完成”的边界。

## 建议并行方式

### Thread A

实名认证闭环

### Thread B

邀请裂变与邀请资格闭环

### Thread C

会员能力与模板配置闭环

### Thread D

AI 简历润色闭环

## 统一收口要求

并行产出完成后，主线程统一做三件事：

1. 校正四张切片卡的字段结构和深度
2. 拉通它们之间的前置依赖
3. 回填 `00-28/tasks.md` 的下一轮执行项
