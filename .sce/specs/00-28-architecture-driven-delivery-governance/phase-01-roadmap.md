# 00-28 Phase 01 路线图

## 目标

把 `00-28` 从“推进原则”推进到“可直接排开发、可持续回填状态”的第一轮执行包。

本轮仍以 4 个能力切片作为统一推进入口：

1. 实名认证闭环
2. 邀请裂变与邀请资格闭环
3. 会员能力与模板配置闭环
4. AI 简历润色闭环

## 本轮交付物

- `slices/verify-capability-slice.md`
- `slices/invite-referral-capability-slice.md`
- `slices/membership-template-capability-slice.md`
- `slices/ai-resume-polish-capability-slice.md`
- `execution/verify/*`
- `execution/invite/*`
- `execution/membership/*`
- `execution/ai-resume/*`
- `status/verify-status.md`
- `status/invite-status.md`
- `status/membership-status.md`
- `status/ai-resume-status.md`

## 当前结果

截至 `2026-04-02`，Phase 01 已完成两层沉淀：

1. 4 张能力切片卡已产出
2. 4 组前端 / 后端 / 后台 / 联调执行卡已产出
3. 4 份状态回填文档已建立，开始记录真实联调结论与验收状态

当前四个能力切片的真实基线如下：

| 能力切片 | 当前判定 | 当前结论 |
|----------|----------|----------|
| 实名认证闭环 | 局部完成 | 小程序认证页、后台审核页和演员端公开接口已具备，待真实联调验证 |
| 邀请裂变与邀请资格闭环 | 局部完成 | 前台邀请页、后台治理、演员端 `/invite/*` 查询接口和注册绑定落库已具备，待邀请生效与资格闭环联调 |
| 会员能力与模板配置闭环 | 局部完成 | 后台治理、演员端 `/level/info` 能力摘要、`/card/*`、`/fortune/*`、`/actor/profile/*` 与 `/actor/{id}` 最小输出已具备，小程序 `actor / ai / fortune` 真接口已放开，核心页面已去掉主要等级 / 会员硬编码，但仍未完成真实联调 |
| AI 简历润色闭环 | 局部完成（最小权威接口已落位） | `/ai/quota` 与 `/ai/polish-resume` 最小真接口已补齐，名片页已切到真实配额，但编辑页 patch 流程、后台治理和真实 AI 生成仍未完成 |

按架构反推后的真实开发顺序，仍建议保持：

1. 实名认证闭环
2. 邀请裂变与邀请资格闭环
3. 会员能力与模板配置闭环
4. AI 简历润色闭环

## 下一步建议

- 第一优先级：从“实名认证闭环”进入真实联调，基于已补齐的演员端公开接口跑一次提交 / 审核 / 回写回归
- 第二优先级：补齐邀请记录生效 / 资格发放事实流转，并完成第一次真实联调
- 第三优先级：基于已补齐的 `actor/profile`、`/level/info` 能力摘要、`/card/*`、`/fortune/*` 与 `/ai/*`，跑一次会员 / 邀请 / 名片真实联调并回填结论
- 第四优先级：AI 简历继续冻结 patch 协议、编辑页应用流和后台治理，再从“最小权威接口”推进到真实生成闭环

## 本轮推进原则

1. 每张切片卡都必须覆盖数据、后端、后台、小程序、联调、验收。
2. 每张执行卡都只负责一个交付面，但必须对齐同一张能力切片。
3. 每轮实现后必须回填 `status` 文档，而不是只改任务勾选。
4. 若某条能力本轮只能做到局部完成，必须明确“局部完成”的边界。

## 建议并行方式

### Thread A

实名认证闭环真实实现与第一次联调

### Thread B

邀请裂变演员端契约收口与第一次联调

### Thread C

会员 / 模板演员端输出模型与前台去本地规则化

### Thread D

AI 简历 patch 协议、最小后端适配层与编辑页入口

## 统一收口要求

并行产出完成后，主线程统一做四件事：

1. 校正四份状态文档中的“当前判定 / 联调结论 / 验收判断”
2. 回填执行卡实际完成项和遗留阻塞项
3. 更新 `00-28/tasks.md` 的下一轮执行项
4. 只有在六条闭环条件同时满足时，才允许把状态从“局部完成”改成“闭环完成”
