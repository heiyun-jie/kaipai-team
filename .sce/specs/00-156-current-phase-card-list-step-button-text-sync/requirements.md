# 00-156 需求

## 目标

修复 `pkg-card/card-list/index` 底部主按钮文本没有跟随当前步骤变化的问题。

## 问题

- 页面已经进入 `STEP 03 预览/保存`。
- 底部提示已显示 `步骤 03`。
- 主按钮仍显示 `下一步：上传作品`。

## 验收标准

- `STEP 01` 主按钮显示 `下一步：上传作品`。
- `STEP 02` 主按钮显示 `下一步：预览/保存`。
- `STEP 03` 主按钮显示 `保存并预览`。
- 按钮文本必须跟随 `currentStep` 同步更新。
- 类型检查、小程序构建、包体审查通过。
- 源码和构建产物审查通过，评分不低于 95。
