- [x] T1 新增独立 `00-92` Spec，并把当前问题明确收口为 `dashboard/analytics` 的 `funnel` tab，不继续混入 `00-91`
  - **Validates: Requirements 3.1 R1-R3, 3.4 R9-R11**
- [x] T2 在 `DashboardAnalyticsView.vue` 中完成 funnel tab 单张全宽漏斗板与内联解读收口，不改真实接口
  - **Validates: Requirements 3.2 R4-R6, 3.3 R7-R8**
- [x] T3 完成 `type-check / build / runtime(browser)` 三层验证，并把 `dashboard/analytics` funnel 修复前后证据回填 `execution.md`
  - **Validates: Requirements 3.1 R2-R3, 3.4 R10-R11**
